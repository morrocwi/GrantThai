# network/

**Phase 0 stub.** Opt-in network/directory schemas (expert and mentor directories). Ships EMPTY (schema only) per the founder's letter section 14; populated only after K11 is decided.

Ships: v0.5 (schema only until then). See the repository root README.md and GRANTTHAI_STANDALONE.md
for the full system description, and docs/design/PLAN.md for the
design plan this scaffold follows (where they differ, spec/ wins).
