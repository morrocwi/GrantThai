# tests/fixtures/negative/gitleaks/

No secret-shaped value is committed here. The seeded bad fixture for the
gitleaks guard is generated at test run time by
`tools/ci/make_negative_fixtures.py gitleaks <dir>` (a random
AWS-access-key-shaped string). See `tools/ci/run_all_guards.sh`.
