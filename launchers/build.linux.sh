#!/usr/bin/env bash
set -euo pipefail
# launchers/build.linux.sh — Phase 0 stub. Double-click wrapper (via a
# .desktop launcher, once shipped) around: grantthai validate && grantthai build
echo "GrantThai launcher stub (Linux). CLI implementation ships v0.1."
