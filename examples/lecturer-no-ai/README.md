# examples/lecturer-no-ai/

**FICTIONAL.** Every value in this example is synthetic. No real place
name appears together with a real institution name, per PRIVACY.md.

Phase 0 stub: `project.yaml` here is a placeholder using the FICTIONAL
fund profile (`funds/example/FICTIONAL_CALL@0.1/`). A full worked example
(all chain stages populated, passing `grantthai validate` and `grantthai
build` with `BLOCK == 0`) ships in v0.1, backing acceptance tests AT-2 and
AT-2b (see docs/BUILD_GUIDE.md).
