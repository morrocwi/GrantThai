"""grantthai.assist — OPTIONAL package (Phase 0 stub, no implementation).

Ships v0.3 (assist) / v0.4 (mcp, api). Never loads in the core path; the
core packages (grantthai.core, .validators, .review, .fund, .mapping,
.render, .interview, .cli) never import this package. Nothing in this
package may set a field's status above DRAFT or submit anything
(spec/common/status_permissions.yaml, 'hard_ceiling').
"""
