# Deviations from the original handoff package

`docs/design/PLAN.md` is the design plan this scaffold follows (a
historical record; where it and `spec/` differ, `spec/` wins). Several of its
provisions intentionally depart from the original AI handoff package
(`00_READ_FIRST_AI_BUILD_BRIEF.md`, `core/01`–`06`). This file is the single
place those departures are recorded, per plan section E ("These names
deliberately depart from 01:626-636 ... The departure is recorded in
`docs/deviations.md`") and section I ("Recorded deviations from the
package").

| Deviation | Original (package) | GrantThai (this repo) | Why | Where decided |
|---|---|---|---|---|
| MCP implementation timing | v0.1 (B:134, B:194) | v0.4; only the contract (`spec/mcp/tools.schema.json`) ships early | v0.1 scope held to the lecturer no-AI path (P3); MCP is an optional interface layer, never required for a human to finish a project | plan §D, §G, §I |
| SHACL timing | v0.1 (01 §63 item 6) | v0.2; generated from `spec/common/chain.yaml`, never hand-maintained | Reduces validation engines from three to two in v0.1; a founder sign-off (K15) is requested for this deferral | plan §E "Validation engines", §K15 |
| AI routing timing | v0.2 (01 §64) | v0.3, after the no-AI citizen path ships | The no-AI citizen path (v0.2) must exist and be tested before any AI-assisted routing is introduced, per the amended version order | plan §I |
| `ai_assisted_fill` default | `true` (01:558-563) | `false`; becomes `true` only on explicit user opt-in | Prevents AI-assisted browser fill from being silently on by default | plan §F |
| Status name `SOURCE_CHECKED`/`METHOD_CHECKED` | let AI set these "checked" states (01:626-636) | renamed `STRUCTURE_CHECKED` ("schema-valid; source reference present and resolvable" — does not mean the source supports the value) and `LOGIC_LINKED` ("required chain links exist" — does not mean the method fits); only a deterministic validator pass sets either | Prevents status overclaim; a "checked" status must never imply more confidence than a schema/logic pass actually gives | plan §E |
| `VERIFIED` on self-review | allowed VERIFIED to render as-is regardless of who checked it | `VERIFIED` with `independence: self` renders as `AUTHOR_CHECKED`; only `VERIFIED(independent: <role>, <date>)` renders as written | Prevents self-review from reading as independent verification | plan §E, critic-parity finding 6/7 |
| "AI-SUPPORTED TRANSLATION" / "AI is a translator, scaffold, router" (brief B:12-25) | AI positioned as the headline of the canonical chain | Person-first translation; AI-assisted is explicitly optional; the word "router" is dropped from all repo prose | AI must never become the de facto requirement in practice (P3); the founder ruled the neutral philosophy wording on 2026-09-25 (K1) | plan §A |
| Version/phase order | (implicit) AI-assisted features earlier, citizen features later | AI-free Expert → AI-free Citizen → AI → interfaces → real funds | Founder amendment: AI is optional at every step, so every core path must be proven AI-free before any AI-assisted feature ships | plan §I, §L "Other chair changes" |
| AT-1 acceptance test placement | implicitly tied to v0.1 in the draft | moved to v0.2 (Citizen Mode) | AT-1 as written concerns the citizen/no-PI path, which ships in v0.2, not v0.1 | plan §L |
| Review-gate prefix | package/letter used "G0-G4" | renamed to `RG0`-`RG4` | Avoids a naming clash with the G001-G003 geography validation rule family | plan §E, critic-feasible finding 5 |
| "Signed" review records | letter referred to "signed" records | renamed "named review records"; there is no cryptographic signing in v0.x | Overclaim correction — nothing is cryptographically signed yet | plan §E, critic-publish finding 14 |
| Founder's term "verified project object" | used freely | kept only in `docs/lineage.md`; public prose says "checked against GrantThai rules" instead | Prevents implied third-party verification/endorsement | plan §E |
| Thai NRIIS field labels | implicitly sourced from the screenshot-derived analysis file | `LABEL_TH: NEEDS_VERIFICATION` until captured from a public call/TOR document | Founder ruling K14 (2026-09-25): labels from the screenshot-derived readout are not used; labels and tab order come from public documents later | plan §E, §H, §K14 |
| `direct_submit` | not explicitly fixed as always false in the package | always `false`; `human_final_approval_required` always `true` | P9 "Humans decide" — GrantThai never submits on a person's behalf | plan §A (P9), §F |
| Cold-read acceptance check | "two equal acceptance checks: a human cold read and an AI cold read" (plan §F, B:215) | release QA only: a human cold read is required; an AI cold read is optional and additional. No cold read is ever a condition for a user to trust or use their own file | Founder ruling 2026-09-25: AI stays optional everywhere, so no check may require an AI | `GOVERNANCE.md` founder decisions log |
| `profile.yaml` as an architecture input | plan §C draws Profile as an arrow into the research core | `profile.yaml` is an editor convenience only; identity/team data used in the submission lives in `project.yaml` (`PROFILE.*`); `grantthai build` never reads `profile.yaml` | Keeps the one-input contract exact | `spec/contracts/one-input-one-output.md` |
| Independent check for releases | plan §H step 4: "AI review never counts as the independent check" | a checker distinct from the maker (a different person or a different, fresh AI agent) plus the human founder's approval of every public release | Founder ruling 2026-09-25 | `GOVERNANCE.md` |
| Public status wording | "validated against GrantThai rules" (plan §E) | "checked against GrantThai rules" / "structure-checked" | Avoids overclaim: nothing is validated by an outside party | `spec/common/status.yaml` |
| Structured-field keys | core/02 item keys such as `activity_id`, `progress_percent`, `calculated_total`, `official_nriis_item_label`, `linked_*_ids` | every item has `id`; `progress_percent` -> `weight_percent`, `calculated_total` -> `line_total`, `official_nriis_item_label` -> `item_label`, `linked_<x>_ids` -> `<x>_ids`; full rename list in each `$defs` description of `spec/registry/structured_fields.schema.json` | One uniform link representation that a validator can resolve and a lint can check; fund rules (`allowability_status`, `funding_ceiling_rule`) stay only in the bound fund profile (P6) | `spec/common/links-and-sources.md` |
| Links on both sides | core/02 lists the same link on both ends (e.g. `objective.linked_activity_ids` and `activity.linked_objective_ids`) | a link may be written on either side; the project chain graph is the set union, so a link written twice is one edge | Removes the need to keep two lists in sync | `spec/common/links-and-sources.md` |
| One hash for everything | plan §E: one `project_object_sha256`, excluding only `review_records` and `lock` | two hashes: `content_sha256` (excludes statuses, review records, lock, mapping acceptance) for reviews and the lock, and `state_sha256` (the whole file) for output traceability | With statuses inside the hash, every review went stale the moment its status promotion was written, so the LOCK gate could never be met | `spec/common/object-hash.md` |
| "Conflict log" in the output appendix | plan §G listed "the conflict log"; core/02 §3 field_state had `conflicts: []` | first removed (nothing defined it); since v0.1.0 restored as a **defined** appendix section "Conflicts and open contradictions": the package-level register `registry/contradictions.yaml` plus each record's `conflicts` list (`spec/common/field_record.schema.json`) | Conflicts must be surfaced, never merged (plan §E), so they need a defined place | `spec/output/nriis-submission.contract.md`, `docs/contradictions.md` |
| Label of `CORE.RESEARCH.INNOVATION` | core/05 label "Innovation / Novelty Claim" | "Innovation / What Is New" | The descriptive label should not read as the project's own priority claim | independent pre-publish review |
| `grantthai init` | `init --role --lang` writes a separate `profile.yaml` and workspace | `init [PATH] [--project-id] [--fund] [--mode]` writes a blank `project.yaml` (refuses to overwrite) | `project.yaml` is the only input `build` reads; a blank project is the useful starting point for the CLI, the skill, MCP and the HTTP API | v0.1.0 engine |
| MCP and REST timing | v0.4 "interfaces", after v0.3 AI assist | shipped in v0.1.0 as thin wrappers over `grantthai.api_py`, capped at `DRAFT` in the core; web form, launchers and Citizen Mode deferred | Founder scope change 2026-09-25 ("เอาแค่ สกิล mcp และ api ...") | `GOVERNANCE.md` founder decisions log, `docs/BUILD_GUIDE.md` |
| X003 timing | family X ships v0.2 | X003 (AI draft marked `SOURCE`) evaluated in v0.1.0 on every record, not only Evidence | Founder knowledge principle: an AI never marks its own prose as `SOURCE`; a hand- or chat-written `project.yaml` does not pass through `set_field` | `validators/rules.yaml`, `src/grantthai/validators/engine.py` |
| Origin types | core/02 §2: every field declares one of five origins; core/05 has no origin and marks research-core fields OBSERVED | the Phase 0 registry dropped origin (a silent reconciliation found by the 2026-09-25 audit); **since v0.1.0** every registry record has `origin` + `origin_basis`, only `NRIIS_NATIVE` fields are placed on a tab, and the core/05 vs core/02 conflict is recorded as CX-01 | core/02 R4: authoring-core fields must never be presented as NRIIS fields; the output contract already required an ORIGIN column | `registry/fields.jsonl`, `docs/contradictions.md` |
| Package contradictions | core/02, core/05 and core/06 disagree on origins, types and edge directions | recorded in `docs/contradictions.md` / `registry/contradictions.yaml` as OPEN with both readings; `chain.yaml` no longer calls them "direction choices" | Contradictions are surfaced, never merged; a direction change needs a decision | `docs/contradictions.md` |
| Standalone file name | `OPEN_THAI_RESEARCH_STANDALONE.md` (core/01 §5) | `GRANTTHAI_STANDALONE.md` | Repository name is GrantThai | Phase 0 |
| Researcher profile keys | core/02 §5 00.01–00.05 (identity, affiliation, expertise, scholarly identity, credentials) | name/organisation/ORCID on team members; since v0.1.0 also `PROFILE.TEAM.EXPERTISE` (00.03 + SD-1 p15) and `PROFILE.TEAM.CREDENTIALS` (00.05). Scholarly metrics (00.04: citations, h-index) stay out | core/02 §4B: prove fit, not prestige; metrics without relevance are to be omitted | `registry/fields.jsonl` |
| Bridge nodes | plan: 11 bridge nodes | 5 in `spec/common/chain.yaml` | The rest belong to the v0.2 bridge ontology | `spec/common/chain.yaml` notes |
| Candidate Thai labels | plan §I: labels in v0.5 | v0.1.0 carries **candidate** labels read from public documents (SD-1, SD-4), every one `NEEDS_VERIFICATION` and shown tagged "candidate"; registry `label_th` stays `NEEDS_VERIFICATION`; nothing from the screenshot-derived readout | Answer the real form as completely as the public sources allow without declaring anything official (K14) | `mappings/nriis/labels@nrct-manual-2566.yaml` |
| Form types and funding-unit templates | SD-1 p47: five proposal form types; funding-unit Word/PDF templates uploaded to NRIIS (SD-2 p9, SD-3 p9) | **shipped in v0.2** as form profiles (`mappings/nriis/form_profiles/`, 8 profiles, all `NEEDS_VERIFICATION`; `form_profile` in `project.yaml`, null = the observed form). The five SD-1 profiles and the two template profiles add no field-level difference beyond what a page supports; `ff_full_proposal@nriis-2570` (SD-5) lists extra items and candidate budget rules, never evaluated | Form profiles need their own contract and sources | `spec/mappings/form_profile.schema.json`, `src/grantthai/mapping/form_profile.py` |
| Project lifecycle | core/04: 20-state lifecycle, V-L01–V-L06 (contract, reports, 5-year utilization reporting) | **deferred to v0.2** (or declared out of scope then); crosswalk marks V-L01–V-L06 `none` | v0.1 covers the proposal only | `validators/crosswalk.yaml`, `docs/BUILD_GUIDE.md` v0.2 |
| Writing layer | core/02 §4A.2, §4B, §4E, §15; core/01 §30 copy/paste checklist; SD-4 p166 validation page | **shipped in v0.2** (`guidance/writing_intent.yaml`, rules W101/W102 at REVIEW, output section 4.6 completeness checklist). core/01, core/02 and SD-4 are not in the public repo, so targets citing them are relayed and `NEEDS_VERIFICATION`; the summary and keyword caps were read on SD-5 p3 | Scope of v0.1.0 | `spec/guidance/writing_intent.schema.json`, `src/grantthai/guidance/writing.py` |
| Classification compatibility | core/05 VAL.012, core/02 V-C01–C03 | rules C001–C003 exist but ship v0.2 (INFO in v0.1) | A meaning comparison needs an agreed topic → OECD mapping | `validators/rules.yaml` |
| The word "router" (K-R1) | brief B:12-25 "AI is a translator, scaffold, router"; plan §A and this file (row above, "AI-SUPPORTED TRANSLATION") dropped the word from all repo prose | **re-allowed in a different, defined sense** (v0.3, 2026-09-25): a router is a deterministic **output route chosen by a person** — declared in `work.yaml` (`routing`) or on the command line (`--route`); no AI and no tool ever picks a route, and an ambiguous resolution stops with the candidate list. The old sense (AI deciding where a person's work goes) stays dropped | Founder reframe 2026-09-25: entering NRIIS is one option of a router, because GrantThai opens to academic articles as well ("การลงใน NRIIS ไม่ใช่แกนหลักอีกต่อไป แต่เป็นแค่ทางเลือกหนึ่งของ router เพราะเราจะเปิดให้ตั้งแต่การทำบทความวิชาการด้วย") | plan §N, `spec/contracts/one-input-one-output.md` 0.3, `routes/INDEX.yaml` |
| One output vs one output per route | plan §A, §F and the 0.2 contract: exactly one output, `build/NRIIS_SUBMISSION.md`, with the concept note as "the one exception" | one input (`work.yaml` 0.3, a superset of `project.yaml` 0.2; legacy files read unchanged), one command per route, exactly one file per route: `NRIIS_SUBMISSION.md`, `ACADEMIC_ARTICLE.md`, `RESEARCH_CONCEPT_NOTE.md`; the concept note is an ordinary route. `routing` is outside `content_sha256` (K-R3); `fund_binding` is required only by routes with `needs_fund_binding`; the new origin `VENUE_NATIVE` (K-R2) marks fields a publication venue type asks for | The NRIIS goal is unchanged for its route (AT-R1 byte-identical); the reframe adds routes beside it | `spec/contracts/one-input-one-output.md` 0.3, `spec/work/work.schema.json`, `spec/common/object-hash.md`, `docs/BUILD_GUIDE.md` "v0.3 router" |
| TOKE command name `route` | TOKE integration spec §4: `grantthai route --claim-type`, `route_candidates()` | renamed `grantthai advise infra --claim-type`, `api_py.advise_infra()`, MCP `grantthai_advise_infra` (not built in the router waves) | "route" means output route only (K-R8: router first, TOKE rebased onto it) | plan §N |

See also `GOVERNANCE.md` "Recorded dissent" for decisions where a critic
finding was only partly applied, and `docs/design/PLAN.md` §L for the
full change log against the original draft plan.
