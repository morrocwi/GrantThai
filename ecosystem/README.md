# ecosystem/

Machine-readable ecosystem data:

- `ecosystem.yaml` — the two nested ecosystems (Toledo conceptual framework;
  Thailand R&I ecosystem), where GrantThai sits, the actor table, and
  sibling-infrastructure pointers. Companion to `docs/ecosystem.md`
  (the full narrative, TH+EN, with diagrams).
- `positions@2026-09.yaml` — optional, dated, `RELAYED` ecosystem/policy
  position picklist (plan section B, P8: never forced): the six policy
  lenses of core/02 §4A.1 (the option list of
  `CORE.ALIGNMENT.POLICY_PATHWAY`) and the eleven ecosystem positions of
  core/02 §4G. Populated in v0.1.0; every entry `NEEDS_VERIFICATION`.

Both files carry `derived_from: core/03@sha256:...` lineage and are
explicitly dated/`RELAYED` — never treated as settled, current fact. See
`docs/sources.md` for verification status.
